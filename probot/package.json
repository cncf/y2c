{
  "name": "y2c-probot",
  "main": "functions.js",
  "scripts": {
    "dev": "nodemon",
    "start": "NODE_PATH=./ probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "@google-cloud/datastore": "^5.0.3",
    "@octokit/auth-oauth-app": "^2.2.1",
    "@probot/serverless-gcf": "^0.2.0",
    "googleapis": "^46.0.0",
    "probot": "^9.5.3"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "nock": "^11.4.0",
    "nodemon": "^2.0.0",
    "smee-client": "^1.1.0",
    "standard": "^14.3.1"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "nodemonConfig": {
    "exec": "yarn start",
    "ignore": [
      "node_modules/"
    ]
  },
  "jest": {
    "testEnvironment": "node"
  },
  "exports": {}
}
